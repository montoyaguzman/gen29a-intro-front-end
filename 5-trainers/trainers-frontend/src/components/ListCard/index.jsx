import './index.css';

function ListCard({ children }) {

    // const children = props.children;
    // const { children } = props;

    return (
        <>
            {children}
        </>
    );
}

export { ListCard };